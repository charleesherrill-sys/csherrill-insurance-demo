Customers are reporting that the claim-detail page sometimes shows another member's claim, and the claims list and billing pages have gotten painfully slow as data has grown. Investigate the claim-detail endpoint for a broken-access-control / IDOR issue where one authenticated user can read another user's claim by changing the claim ID, and investigate the N+1 query pattern in the claims-list and billing paths. Produce a plan to (1) enforce ownership/authorization on the claim-detail endpoint, and (2) eliminate the N+1 queries, then implement the fix and add regression tests that prove both the access-control fix and the query-count reduction. Reproduce both issues first, then fix, then verify.
